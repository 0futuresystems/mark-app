// Simple script to create test data via the browser
console.log('Navigate to http://localhost:5000/test-setup and click "Create Test Data"');
console.log('Then navigate to http://localhost:5000/review to see the thumbnails');
console.log('Check console for [LotThumbnail] and [normalizeBlob] logs');
